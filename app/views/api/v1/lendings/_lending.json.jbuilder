# 部分テンプレート(会社)

# company 変数は、
# 部分テンプレートの呼び出し元で渡された引数。
json.user_id(lending.user_id)
json.item_id(lending.item_id)
json.dead_line(lending.dead_line)
json.is_lent(lending.is_lent)
json.created_at(lending.created_at)
json.updated_at(lending.updated_at)
